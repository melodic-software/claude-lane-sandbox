# Stack pilot

Layer 1 — data: the fixture's stacked-pull-request probe surface.
Layer 2 — api: a second layer that only makes sense once layer 1 exists.
Layer 3 — ui: the top layer, reviewable on its own diff.
